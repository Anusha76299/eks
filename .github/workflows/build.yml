name: Build_TF_Plan
 
on:
  push:
    branches:
      - main
 
jobs:
  build-plan:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: 'us-east-1'
 
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
 
      - name: Terraform Init
        id: init
        run: terraform init
      
      - name: Terraform Validate
        id: validate
        run: terraform validate
 
      - name: Terraform Plan
        id: plan
        if: github.event_name == 'push'
        run: terraform plan -no-color
        continue-on-error: true
 
      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
         terraform apply -auto-approve  
         
      - name: Get Terraform Outputs
        id: terraform_outputs
        run: |
         output_response=$(terraform output -raw instance_ips )
         instance_ips=$(echo "$output_response" | cut -d' ' -f1)
         echo $instance_ips
         echo "::set-output name=instance_ips::$instance_ips"
         
      - name: Download PEM file from S3
        env:
            BUCKET: ${{ secrets.BUCKET }} 
        run: |
          aws s3 cp $BUCKET .

      - name: Start Bookstack Application
        run: |
         instance_ips=${{ steps.terraform_outputs.outputs.instance_ips }}
         echo "Instance IPs: $instance_ips"
